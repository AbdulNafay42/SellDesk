{
  "name": "selldesk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
